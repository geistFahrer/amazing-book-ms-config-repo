server:
  servlet:
    context-path: /bookms
  port: 8888

message: "Hello from book-test-service"

spring:
  application:
    name: book-service
  profiles:
    active: dev
  config: 
    import : "optional:configserver:"
  cloud:
    config:
      uri: http://localhost:8070